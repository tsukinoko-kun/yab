name: Test

on:
  pull_request:
    branches:
      - main
    paths:
      - "**.go"
      - "go.mod"
      - "go.sum"
  workflow_dispatch:

jobs:
  test-mac:
    runs-on: macos-latest
    steps:
      - name: Setup Go environment
        uses: actions/setup-go@v4.1.0
        with:
          go-version: ">=1.22.0"
          cache: true
          architecture: x64
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build
        run: go build ./...
      - name: Test
        run: go test ./... -v
